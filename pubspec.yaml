name: weather_animation
description: The package contains beautiful weather animations. Easy setup with the weather configurator!
homepage: https://t.me/+AkGV73kZi_Q1YTMy
repository: https://github.com/PackRuble/weather_animation
issue_tracker: https://github.com/PackRuble/weather_animation/issues

version: 1.0.0

dependency_overrides:
  # todo: remove when updated build_runner
  #  Error: The class 'FileSystemEvent' can't be extended, implemented,
  #  or mixed in outside of its library because it's a sealed class.
  watcher: 1.1.0

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  json_annotation: ^4.8.1
  freezed_annotation: ^2.4.1

dev_dependencies:
  freezed: ^2.4.3
  json_serializable: ^6.7.1
  build_runner: ^2.4.6
  lint: ^2.1.2

screenshots:
  - description: 'Preset weather scenes'
    path: images/all_scenes_scr.png
  - description: 'Weather configurator'
    path: example/weathunits_configurator/assets/weather_configurator_screen.png

funding:
  - https://t.me/+AkGV73kZi_Q1YTMy

topics:
  - weather
  - animation
  - ui
  - widgets
  - with-playground